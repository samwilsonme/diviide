// The license page's visible "Last updated" date. Single source shared with
// the private site (which shows it in its own page header), so the two repos
// can never drift. The license texts themselves live in LicenseContent.astro.
export const LICENSE_UPDATED = 'August 1, 2026';
